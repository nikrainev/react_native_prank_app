import React from 'react';
import {
  SafeAreaView,
} from 'react-native';
import { MainPage } from './container/MainPage';

const App = () => {

  return (
    <SafeAreaView>
        <MainPage />
    </SafeAreaView>
  );
};

export default App;
